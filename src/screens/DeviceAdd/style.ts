import { StyleSheet } from "react-native";


export const DeviceAddStyle = () => {
    return StyleSheet.create({
        container : {
            flex: 1,
            width: '100%',
        },
    });
};